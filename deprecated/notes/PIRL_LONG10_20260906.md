# Latest requested experiment: Long-only PPO, 10 iterations

Supersedes the130-task training and four-suite continuation. Those trainers
were explicitly stopped, with logs/checkpoints retained. HF Plus evaluations
on GPUs0–3 are independent and continue.

GPUs4–7, fresh RLinf-Pi05-LIBERO-SFT initialization, seed42. Standard libero_10
only (10 Long tasks). Flow-SDE PPO, frozen VLM, train expert and VLM-attached
critic; no CSD, no diagnostic CSD, no LoRA. Prediction/execution10/10, denoise5,
noise.5, training horizon480, 64envs,8 rollout epochs,4 PPO update epochs,
global2048, actor LR5e-6, critic1e-4, gamma.99, lambda.95, PPO/value clip.2,
gradient clip1, Adam(.9,.95), eps1e-8, decay.01.

Sources: pi_RL https://arxiv.org/html/2510.25889 Appendix J Table11, and local
examples/embodiment/config/libero_10_ppo_openpi_pi05.yaml. Paper's full training
budget is500; user truncates at10. Keep cosine schedule500 (min ratio.1), do
not compress it to10. Local upstream config uses1000; this run retains the
paper-budget500 adaptation documented previously.

Memory/runtime adaptations: microbatch16 with gradient accumulation retaining
global2048, rollout offload, no bootstrap overlap. No gradient checkpointing.
Checkpoint saved at10, then stop training and automatically evaluate Long on
the same four GPUs:500 episodes (50 fixed trials/task), ODE,10/10,5 denoise,
standard benchmark eval horizon520,20 envs,25 epochs. No training afterward.
TensorBoard and online W&B project rlinf-ppo-csd. Original disk watchdog retained.

Launcher: scripts/run_pirl_long10_20260906.py; reuses validated standalone config
libero_130_ppo_pi05_gpu47_20260906 with explicit Long overrides. Exact launch
command and resolved config saved under logs/20260906-pirl-long10/train/.
Output: logs/20260906-pirl-long10/step10_eval/results.json.

## GPU0–3 rerun

User requested a fresh PPO-Long pi05 rerun on GPU0–3 with10/10 on2026-09-06.
Launch scripts/run_pirl_long10_20260906.py --gpus 0-3. This keeps the above
10-iteration sanity budget and automatic500-trial Long evaluation, both on0–3.
All training hyperparameters remain unchanged. This is the selected paper-aligned
recipe, not a claim that an exhaustive settings search established it as best.
Fresh output logs/20260906-pirl-long10-gpu03, experiment suffix_gpu03.
The earlier stopped run is preserved, and ManiSkill on4–7 is untouched.
