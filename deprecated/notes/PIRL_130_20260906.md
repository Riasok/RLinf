# PPO-130 and official Plus-Spatial comparison — 2026-09-06

## Updated user direction: stop at10, then sanity evaluation

Training is now bounded to10 runner iterations, saves at10, and skips the
in-training130-episode screen. Optimizer cosine schedule remains500, unchanged.
The initial unbounded attempt had no completed iteration/checkpoint and was
stopped to apply the runtime settings; its unfinished first iteration is repeated.
On normal training completion and worker cleanup, automatically evaluate step10
on standard Spatial/Object (GPUs4–5) and Goal/Long (GPUs6–7), sequentially within
each lane. Each suite:500 episodes (50/task), 20 envs,25 epochs, ODE, generate10,
execute10, denoise5, seed42. Horizons240/320/320/520. These match training's
action/denoising settings rather than changing inference between suites.
No further training is queued. Results: logs/20260906-pirl130/step10_sanity/.
The original500-iteration plan below is retained as provenance and superseded
by this section where it concerns stopping, checkpointing and evaluation.

User request: PPO only, all 130 standard LIBERO tasks, GPUs4–7; evaluate official
PPO-130 and full-shot SFT pi0.5 releases on Plus-Spatial using GPUs0–3.

## Sources and limits

- https://arxiv.org/html/2510.25889 Appendix J Table11 (latest): 500 iterations,
  batch2048, actor5e-6, critic1e-4, gamma.99, lambda.95, PPO clip.2, 64 envs,
  8 rollout epochs. Pi05 prediction horizon10; suite-specific execution5/5/5/10,
  denoise3/5/5/5, updates1/1/3/4, noise.5/.3/.3/.5. Only Long uses a scheduler.
- Original v1 Appendix B Table6 specifies 400 iterations instead of500.
- Upstream examples/embodiment/config/libero_10_ppo_openpi_pi05.yaml supplies
  Adam(.9,.95), epsilon1e-8, weight decay.01, gradient clip1, cosine min ratio.1,
  critic after VLM, frozen VLM and trainable expert, Flow-SDE, no LoRA.
- Neither inspected paper version nor upstream main has a joint pi05 PPO-130
  recipe. https://github.com/RLinf/RLinf/issues/1281 remains unresolved about
  PPO-130 initialization/VLM differences. Do not label this an exact reproduction
  of the HF release, nor treat the two HF releases as a controlled causal ablation.

## Training adaptation

Fresh from existing RLinf-Pi05-LIBERO-SFT (few-shot paper initialization), not
from the official PPO release or full-shot SFT. All130 tasks, no task filter.
Apply Long's shared settings to the joint task pool: prediction/execution10,
denoise5, noise.5, updates4, rollout horizon480, 500 runner iterations with
cosine500. Seed42; random fixed-reset sampling across the130-task pool.
Memory adaptations: microbatch16 with accumulation preserving global2048,
rollout offload enabled, bootstrap overlap disabled. CSD and CSD diagnostics off.
No changes to core model, PPO, or CSD implementation for this launch.

Screen every10 iterations on130 fixed episodes (one/task), horizon520; final
benchmark quality requires additional trials. Save every40 and at final500.
TensorBoard plus online W&B under rlinf-ppo-csd. Watchdog stops only its owned
processes on failure, free data space<80GiB or root/tmp space<5GiB; no retries,
no deletion of checkpoints. Ray spill stored on data volume.
Ray local_fs_capacity_threshold is .995 because this10TB volume is already96%
occupied despite roughly450GB available; our absolute80GiB guard stops sooner
than that Ray cutoff. The initial startup attempt was stopped before any update
to correct this threshold; logs retained under train_startup_capacity_guard.

## Official evaluation

PPO: RLinf/RLinf-Pi05-PPO-LIBERO-130 @df129b1bf47201ab9490d6645dfcc08133cbd689 (GPUs0–1).
SFT: RLinf/RLinf-Pi05-LIBERO-130-fullshot-SFT @6222623f635769bfc73c9472e29fab9b7fd8e027 (GPUs2–3).
Weights and their norm_stats only; optimizer download not needed.
Identical Spatial paper inference: pi05, prediction10/execution5, denoise3,
ODE evaluation, horizon240, seed42, all2402 installed Plus-Spatial variants,
one fixed trial each; 40 envs and61 rollout epochs with padding excluded.
This is coverage of the installed benchmark variants, not 50 trials per variant.
Local TensorBoard and separate result.json/provenance files.

Entrypoint: scripts/run_pirl130_20260906.py {train,ppo,sft}.
Artifacts: logs/20260906-pirl130/.

Validation: Hydra composition and RLinf validate_cfg passed; registry confirmed
130 tasks and physical GPU placement4,5,6,7. Both safetensors headers load.
Launcher Ruff passes; existing trial-filter and online-CSD tests:16 passed.
Persistent sessions: pirl130-train, pirl130-ppo-plus, pirl130-sft-plus.
